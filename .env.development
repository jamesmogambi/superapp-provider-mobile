# ── Development environment ─────────────────────────────────────────────
# Loaded automatically when you run `expo start` (NODE_ENV=development).
# Overrides values in .env. Only EXPO_PUBLIC_* vars are bundled into the app.

# Clerk — Development instance.
# Publishable keys are public/safe to commit. Get it from:
# Clerk Dashboard → API Keys (Development instance) → Publishable key.
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_bm90ZWQtY29icmEtOTQuY2xlcmsuYWNjb3VudHMuZGV2JA

# Backend API base URL (placeholder — update when backend integration lands).
EXPO_PUBLIC_API_URL=https://dev-api.foxjek.example.com
